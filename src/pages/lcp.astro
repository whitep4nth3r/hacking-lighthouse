<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Hack LCP score</title>
    <meta name="description" content="How to hack your Google Lighthouse LCP score" />
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>🚨</text></svg>">

    <style is:global>
      html {
        font-size: 100%;
        font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
      }
      
      body {
        background-color: antiquewhite;
      }

      main {
        position: relative;
      }

      section {
        max-width: 768px;
        margin: auto;
      }

      main h2 {
        font-size: 4rem;
        margin-top: 4rem;
        margin-bottom: 4rem;
      }

      main p {
        font-size: 2rem;
        margin-bottom: 2rem;
      }

      main a {
        color: inherit;
        text-decoration: underline;
      }

      .splash {
        position: absolute;
        display: grid;
        place-content: center;
        min-height: 100vh;
        width: 100vw;
        z-index: 10;
        opacity: 1;
        transition: opacity 1s ease-in-out;
        background-color: bisque;
      }

      .splash h1 {
        color: black;
        font-size: 2rem;
      }
    </style>
  </head>
  <body>
    <main>
      <div class="splash">
        <h1>hello</h1>
      </div>
    </main>
    <footer>
      <script is:inline>
        const HTML = `<h2>This page scored 100 on Lighthouse performance</h2><p>This page took 10 seconds to load real content.</p><p>The "hello" message was considered as the LCP element. <h2>Was it useful?</h2><p>No, because it didn't tell you anything was loading, or how long you had to wait. It was one word. It could have been any word. But it passed the test. It also works with a <em>single letter or punctuation mark</em>.</p><p><strong>Test the page yourself using Chromium dev tools in an incognito window.</strong></p><p><a href="">TODO: add link | Read the full article about how you can hack your Lighthouse performance score.</a></p>`;
        setTimeout(() => {
          const main = document.querySelector("main")
          const insert = document.createElement("section")
          insert.innerHTML = HTML;
          main.appendChild(insert);

          const splash = document.querySelector(".splash");
          splash.style.opacity = "0";
        }, 10000);
      </script>
    </footer>
  </body>
</html>
